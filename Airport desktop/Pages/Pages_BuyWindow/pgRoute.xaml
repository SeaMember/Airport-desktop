<Page x:Class="Airport_desktop.Pages_BuyWindow.pgRoute"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Airport_desktop.Pages_BuyWindow"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="pgRoute">

    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0"
                    VerticalAlignment="Center">
            <Label>Откуда:</Label>
            <ComboBox Style="{StaticResource cbxRoute}"
                      x:Name="cbxStart"
                 ></ComboBox>
        </StackPanel>
        <StackPanel Grid.Column="1"
                    VerticalAlignment="Center">
            <Label>Куда:</Label>
            <ComboBox Style="{StaticResource cbxRoute}" 
                x:Name="cbxEnd"   ></ComboBox>
        </StackPanel>
        <Button VerticalAlignment="Top"
                HorizontalAlignment="Right"
                Grid.Column="1"
                Margin="10" Click="Button_Click">Далее</Button>
    </Grid>
</Page>
